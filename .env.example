# Copy this file to .env and fill in real values once you receive
# the database credentials. Never commit the actual .env file —
# it's already in .gitignore.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=campusbridge_db

SECRET_KEY=change-this-to-something-random
DEBUG=True
PORT=5000
